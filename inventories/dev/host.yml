---
all:
  hosts:
    localhost
    localhost.x.net
  vars:
    localhost_variable: some_value
    var1: 1
 children:
   host01:
     hosts:
       1.x.net
       2.x.net
   host02:
     hosts:
       localhost
     vars:
       ansible_connection: local 
